/**
 * Research Tools — Barrel import
 *
 * Importing this module registers all MCP research tools
 * (web_search, web_scrape, browser_navigate) into the global
 * tool registry so they're available to the Security Copilot
 * and research orchestrator.
 */

import "./webSearch";
import "./webScrape";
import "./browser";
